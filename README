Amido Test App for RESTful Webservices

5 statically created users on startup.

This is what the REST API does:

GET request to /api/users/ returns a list of users
GET request to /api/user/1 returns the user with ID 1
POST request to /api/user/ with a user object as JSON creates a new user
PUT request to /api/user/3 with a user object as JSON updates the user with ID 3
DELETE request to /api/user/4 deletes the user with ID 4
DELETE request to /api/user/ deletes all the users
GET request to /health shows whether the application is up or down

base url is Http://localhost:8080/AmidoTestApp/

To run this application in a terminal: mvn spring-boot:run

Server details can be found in the application.yml file

